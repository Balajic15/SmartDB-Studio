# 🧠 JavaFX + JDBC + SQL CRUD Application with AI Integration

## 📋 Overview
This project is a **JavaFX-based desktop application** that provides a **user-friendly interface** for performing **complete CRUD (Create, Read, Update, Delete)** operations on a **SQL database** using **JDBC**.  
It is enhanced with **AI-powered assistance**, **file drag & drop**, **database connection testing**, and **Excel import/export** features — designed for **non-technical users** to easily manage databases visually.

---

## 🚀 Key Features

### 🗂️ CRUD Operations
Perform all SQL operations without writing queries manually:
- **Create** – Add new records to the database  
- **Read** – View records in a table view  
- **Update** – Edit existing entries directly in the UI  
- **Delete** – Remove unwanted data safely  

Supports **SQL Clauses**:
- `WHERE`, `HAVING`, `ORDER BY`, `GROUP BY`, `LIMIT`

### ⚙️ Database Connectivity
- Works with **MySQL**, **PostgreSQL**, **SQLite**, or any **JDBC-supported** database  
- Includes a **Test Connection** button to verify your **username, password, and host details** before connecting  

### 📁 File Drag & Drop
- Simply **drag and drop** `.sql` or `.xlsx` files into the app to:
  - Import SQL queries  
  - Import data from Excel  
  - Run scripts directly in the database  

### 📊 Excel Import/Export
- **Export** your current table view to Excel (`.xlsx`)  
- **Import** data from Excel directly into a database table  
- Uses Apache POI for handling Excel files  

### 🤖 AI Integration
- Built-in **AI assistant** helps users generate SQL queries automatically in plain English!  
  Example:
